services:
  app:
    container_name: vaccine-app
    hostname: app
    build:
      context: app
    volumes:
      - ./app:/usr/src/app/
    networks:
      - app
  api:
    container_name: vaccine-api
    hostname: api
    build:
      context: api
    volumes:
      - ./api:/usr/src/app
    networks:
      - app
    ports:
      - 3000:3000
  mysql:
    image: mysql:8.0
    container_name: vaccine-mysql
    hostname: mysql
    networks:
      - app
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: vaccine
      MYSQL_USER: viruskizz
      MYSQL_PASSWORD: viruskizz
  mssql:
    image: mcr.microsoft.com/mssql/server
    container_name: vaccine-mysql
    hostname: mssql
    networks:
      - app
    environment:
      ACCEPT_EULA: Y
      MSSQL_SA_PASSWORD: root
networks:
  app: